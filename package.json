{
  "name": "sim-cli",
  "version": "1.0.0",
  "description": "CLI simulation tool — moves an object on a table without falling off",
  "main": "dist/index.js",
  "bin": {
    "sim-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "cli": "npm run build && node dist/index.js",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "typescript": "^5.8.3"
  }
}
